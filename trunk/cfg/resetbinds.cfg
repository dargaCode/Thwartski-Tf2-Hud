echo "CONFIG EXECUTED - RESETBINDS"

//reset all my binds every time a class is changed, in case any binds were overridden in a class config

unbindall


//------------------------------------------------------------------------
//			BASICS AND MOVEMENT
//------------------------------------------------------------------------

bind "MOUSE1" "+attack"
bind "MOUSE2" "+attack2"

bind "SPACE" "+jump"
bind "a" "+duck"

//I wouldn't recommend using sdfc for movement for most people, but I have a very special keyboard that it works perfectly for.

bind "s" "+mleft"
bind "d" "+mfwd"
bind "f" "+mright"
bind "c" "+mback"

// Null-cancelling movement script
// (prevents you from pressing two opposing directions, which causes you to stop moving)

alias +mfwd "-back;+forward;alias checkfwd +forward"
alias +mback "-forward;+back;alias checkback +back"
alias +mleft "-moveright;+moveleft;alias checkleft +moveleft"
alias +mright "-moveleft;+moveright;alias checkright +moveright"
alias -mfwd "-forward;checkback;alias checkfwd none"
alias -mback "-back;checkfwd;alias checkback none"
alias -mleft "-moveleft;checkright;alias checkleft none"
alias -mright "-moveright;checkleft;alias checkright none"
alias checkfwd none
alias checkback none
alias checkleft none
alias checkright none
alias none ""


//------------------------------------------------------------------------
//			UI 
//------------------------------------------------------------------------

bind "ESCAPE" "cancelselect"

bind "F6" "save_replay"
bind "F11" "hud_reloadscheme"

bind "j" "cl_trigger_first_notification"
bind "k" "cl_decline_first_notification"

bind "t" "+taunt"

bind "x" "+showscores"

bind "ALT" "dropitem"
bind "SHIFT" "inspect"

bind "b" "changeclass"

bind "`" "toggleconsole"
bind "DEL" "open_charinfo_backpack"
bind "TAB" "open_charinfo_direct"
bind "BACKSPACE" "changeteam"

bind "0" "slot10" //without this, you'll get stuck in menus for server mods, such as !bp and !credits


//------------------------------------------------------------------------
//			COMMUNICATION
//------------------------------------------------------------------------

bind "1" "voicemenu 2 7;say_team Good Job!" 			//good job
bind "2" "voicemenu 0 2" 								//gogogo
bind "3" "voicemenu 1 1" 								//spy
bind "4" "voicemenu 2 0" 								//help
bind "5" "voicemenu 1 3;say_team Need Teleporter!" 		//teleporter

bind "q" "voicemenu 0 1" 								//thanks

bind "ENTER" "voicemenu 0 0" 							//medic

bind "z" "voicemenu 2 1" 								//battle cry

bind "PGUP" "voicemenu 0 6" 							//yes
bind "PGDN" "voicemenu 0 7" 							//no

bind "y" "say"
bind "u" "say_team"

bind "v" "+voicerecord"


//------------------------------------------------------------------------
//			PRACTICE TOGGLE
//------------------------------------------------------------------------

//bind "=" practicetoggle

//alias practicetoggle startpractice

//alias startpractice "exec practice.cfg; say" "Practice Mode Enabled"; mp_waitingforplayers_cancel 1; firstperson; buddha 1; cl_showpos 1; play vo/announcer_am_lastmanalive03.wav; alias practicetoggle stoppractice"

//alias stoppractice "exec resetbinds.cfg; say" "Practice Mode Disabled"; firstperson; buddha 0; cl_showpos 0; play vo/announcer_am_killstreak04.wav; alias practicetoggle startpractice"
