echo "CONFIG EXECUTED - THWARTSKI CLASS DEFAULT"

//every time the player spawns as a new class, this config is called.

//wipe out any custom binds from the previous class's config.
exec thwartski_resetbinds

//reset crosshair settings, in case any changes were made in a class config.
//fixes crosshairs if they've been overridden by watching someone else's firstperson demo.
exec thwartski_crosshairs

//reset the weaponswap scripts, to get them ready for any overrides in the new class's config.
exec thwartski_weaponswap

//clean up from the practice toggle
firstperson
cl_showpos 0
